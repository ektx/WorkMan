# how use

```js
// 默认效果
this.$iziToast.show({
    title: data.mes,
    timeout: 3000
})

this.$iziToast.info({
    title: data.mes,
    timeout: 3000
})

this.$iziToast.warn({
    title: data.mes,
    timeout: 3000
})

this.$iziToast.error({
    title: data.mes,
    timeout: 3000
})
```